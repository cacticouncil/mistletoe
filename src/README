General build requirements:
	Python 2.7.x
	pip install: setuptools, cx_Freeze, PySide, PyWin32 (Windows only)

To build a directory with an executable and other files inside it:
	python setup.py build
	Directory is named "build\exe.win32-2.7\"
	Copy/zip directory to share
	
For a Windows .msi:
	python setup.py bdist_msi
	It's in "dist\"

On Mac OS X, for a Mac disk image: <= UNTESTED
	python setup.py bdist_dmg

Windows installer:
	NSIS Installer (http://nsis.sourceforge.net/Main_Page)
	pip install: bdist_nsi <= UNNECESSARY?

	Right-click and Compile the "Mistletoe.nsi" within "NSIS".

Projects are in Visual Studio and Eclipse formats.